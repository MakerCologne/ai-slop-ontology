#!/usr/bin/env python3
"""Diff-mode scoring (issue #10). Status: stub (Red phase)."""

def diff_scores(base: str, head: str, repo_dir: str = None) -> list:
    return []

def scored_windows(lines, changed_indices, ctx: int = 3) -> list:
    return []
